For overall design decisions

`PATTERN ACTION`    

# Statements
* No {} , we'll be using `;` as delimiter between statements
* String literals must be escaped with `"` or `'`

# Functions
* `print`
* TODO 